export interface Book {
  id: number;
  title: string;
  author: string;
}

export interface User {
  id: number;
  username: string;
  firstName: string;
  email: string;
  lastName: string;
  isVerified: boolean;
}
